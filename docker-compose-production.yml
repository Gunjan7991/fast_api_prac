version: "3.8"
services:
    fastapi:
        build: .
        ports:
            - 8000:8000
        volumes:
            - ./:/usr/src/app

        env_file:
            - ./.env

        command: uvicorn app.main:app --host 0.0.0.0 --port 8000

        depends_on:
            - postgres

    postgres:
        image: postgres:15.4-alpine3.18
        ports:
          - 5433:5433
        
        environment:
            - POSTGRES_USERNAME=postgres
            - POSTGRES_PASSWORD=superStrongpassword1230!
            - POSTGRES_DB=tweeter_clone
        volumes:
            - postgres-db:/var/lib/postgresql/data2

        command: -p 5433

volumes:
    postgres-db:
